#include<stdio.h>
#include<conio.h>
#include<string.h>

void stri(char str[])
{
	int i=0;
	while(str[i]!=)
	{
		printf("%c",str[i]);
		i++;
	}
}

void main()
{
	char str[]={'N','A','M','A','N'};
	stri(str);
}
